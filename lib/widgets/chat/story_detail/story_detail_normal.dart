import 'package:flutter/material.dart';

class StoryDetailNormal extends StatelessWidget {
  const StoryDetailNormal({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("asdasd");
  }
}
